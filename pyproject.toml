[project]
name = "ArtistsForArtists"
dependencies = [
    "flask",
    "flask-login",
    "pandoc",
    "pypandoc",
    "Flask-WTF",
]
version = "2.0"
authors = [
    {name = "Jacob Jirak"}
]
description = "A CMS designed by authors for authors."

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"